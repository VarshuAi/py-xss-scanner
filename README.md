# Python Reflected XSS Scanner
A command-line tool designed to parse form elements from HTML and inject XSS vectors to check for input sanitization.

## Prerequisites
```bash
pip install requests beautifulsoup4
```